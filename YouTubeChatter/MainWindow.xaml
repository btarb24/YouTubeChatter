<Window x:Class="YouTubeChatter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YouTubeChatter"
        mc:Ignorable="d"
        Title="YouTube Chatter" Height="800" Width="700">
  <DockPanel LastChildFill="True" Margin="10">
    <DockPanel DockPanel.Dock="Top">
      <TextBlock Text="Youtube stream URL:"/>
      <TextBox x:Name="txtUrl" KeyDown="TxtUrl_KeyDown" Margin="5,0"/>
    </DockPanel>
    <DockPanel x:Name="dckReply" IsEnabled="False">
      <DockPanel DockPanel.Dock="Top" Margin="0,10,0,0">
        <TextBlock Text="Search: "/>
        <TextBlock x:Name="lblIsLive" Text="[Live]" Foreground="Red" Margin="10,0" DockPanel.Dock="Right" Visibility="Collapsed"/>
        <TextBlock x:Name="lblComplete" Text="[Finished]" Foreground="Gray" Margin="10,0" DockPanel.Dock="Right" Visibility="Collapsed"/>
        <TextBox x:Name="txtSearch"/>
      </DockPanel>
      <TextBox x:Name="txtResults" Margin="0,10,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
               VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
    </DockPanel>
  </DockPanel>
</Window>